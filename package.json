{
  "name": "emberholm-portal-contracts",
  "version": "1.0.0",
  "description": "Emberholm Portal Smart Contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "verify:base": "hardhat verify --network base",
    "test": "hardhat test"
  },
  "keywords": ["NFT", "GameFi", "Base", "Ethereum"],
  "author": "Emberholm Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
